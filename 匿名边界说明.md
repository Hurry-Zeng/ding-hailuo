# “海螺”匿名反馈渠道匿名边界说明

## 一、结论

“海螺”可以实现业务数据层面的匿名反馈，但不能承诺平台审计层面的绝对无痕。

建议管理层将本系统定义为：

> 匿名表达渠道，而不是技术上不可追溯的举报系统。

## 二、系统如何实现业务匿名

当前版本由 H5 页面收集信息，并通过连接流写入 AI 表格。匿名实现方式如下：

1. H5 页面不调用获取用户信息的钉钉 JSAPI。
2. 提交请求不携带姓名、工号、部门、手机号、uid 或 unionId。
3. AI 表格不设置“提交人”“所属部门”“员工编号”等字段。
4. FaaS 写入记录时使用固定的服务账号 `operatorId`，不使用投诉人的 unionId。
5. 提交内容只包含分类和文字描述，不包含图片或附件。
6. 查询码由前端随机生成，仅用于后续关联记录，不包含员工身份。

从业务数据和表格内容角度看，管理员看到的将是一组分类、文字描述、状态、查询码和提交时间，不包含投诉人的员工身份。

## 三、不能承诺的范围

### 1. 钉钉平台访问日志

钉钉客户端、网关、H5 容器、连接平台和开放平台仍可能记录访问时间、设备、网络地址、请求标识等运行信息。特定条件下，这些信息可能被平台或企业管理员用于关联访问人员。

业务代码无法删除或修改钉钉底层审计日志。

### 2. AI 表格操作日志

AI 表格新增记录接口要求 `operatorId`。所有记录都会显示由固定服务账号写入，而不是投诉人写入。

这可以避免记录直接关联投诉人，但平台仍会保留“服务账号执行写入”的审计事实。

### 3. 内容本身泄露身份

匿名提交不代表内容天然匿名。以下情况可能暴露身份：

- 描述中写了姓名、部门、项目名称、具体日程。
- 描述的事件只有少数人知道。
- 描述包含只有本人知道的编号、账号或业务信息。
- 投诉人使用公司统一发放且可关联的号码、邮箱或文件名称。

### 4. 用户自行泄露查询码

查询码是用户后续查询状态的凭证。如果投诉人把查询码发给他人，他人可以凭借查询码关联到对应记录。系统应避免通过查询码返回完整投诉内容。

## 四、系统边界建议

为了让承诺与实际能力一致，建议：

1. 在用户提交页面明确提示：“请自行保存查询码，不要发送给无关人员。”
2. 查询接口只返回处理状态，不返回原始问题描述和分类。
3. 不提供按姓名、手机号、部门或关键词检索投诉人的能力。
4. AI 表格访问权限只授予投诉处理小组的授权人员。
5. 定期审计 AI 表格的访问和导出记录。
6. 前台只展示必要的隐私提醒，不承诺“绝对无法追溯”。
7. 管理层制度中明确：除非涉及重大违法、合规或人身安全风险，不主动使用平台日志尝试识别投诉人。

## 五、建议对外表述

可使用以下表述：

> 本渠道不会向投诉处理表格写入姓名、工号、部门、手机号或钉钉用户标识。系统在业务数据层面保护匿名性，员工无需担心投诉内容自动附带个人身份信息。出于信息安全、审计和平台运行需要，钉钉底层系统仍可能保留访问日志，这部分日志不由本应用控制。

不建议使用以下表述：

- 完全匿名，任何人都无法查明。
- 钉钉后台不会留下任何记录。
- 绝对不会泄露身份。
- 可以放心举报，平台永远无法追溯。

## 六、运营原则

匿名渠道的可信度不仅取决于代码，还取决于后续管理：

- 投诉处理人员不得在内部群中转发原始投诉描述。
- 不在未脱敏的会议材料中引用可识别个人的细节。
- 对查询码进行最小权限管理。
- 对被投诉对象、直接利益相关人实行回避制度。
- 对报复行为建立投诉和处置机制。
- 明确数据保留周期，到期后删除或归档。

只有技术匿名、权限管理和组织制度同时成立，匿名反馈渠道才会被员工真正信任。
