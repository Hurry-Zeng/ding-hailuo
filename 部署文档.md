# 钉钉企业内部 H5 匿名反馈渠道部署说明

## 1. 当前版本范围

当前版本只实现单向提交：

```text
钉钉 H5 页面
  -> 连接流 Webhook
  -> Node.js FaaS 脚本
  -> 写入钉钉 AI 表格
```

当前提交内容只包含投诉分类和文字描述，不包含图片和附件。

暂不实现查询码查询结果返回。“捡海螺”入口保留为后续扩展，当前点击后只提示查询功能暂未开放。

交付文件：

- `index.html`：完整 H5 页面，包含首页、提交页、成功页。
- `dingtalk-faas.js`：连接流 Node.js FaaS 脚本。
- `assets/hero-shell.jpg`：首页主视觉。
- `assets/app-icon.png`：钉钉工作台应用图标。
- `匿名边界说明.md`：管理层说明文档。

## 2. 前置条件

需要以下权限和资源：

- 钉钉组织管理员或子管理员。
- 钉钉开发者后台权限。
- 一个可被员工访问的公网 HTTPS 静态页面地址。
- 一个多维表格，建议使用新版 AI 表格。
- 一个企业内部应用。
- 连接平台使用权限。

注意：连接流不能托管 `index.html`。H5 页面仍需要静态网站、对象存储、企业现有网站或其他公网 HTTPS 地址承载。

## 3. 创建 AI 表格

在钉钉文档中新建 AI 表格，建议字段如下：

| 字段名 | 字段类型 | 说明 |
|---|---|---|
| 分类 | 单选 | 选项：行政、人事、业务、其他 |
| 问题描述 | 文本 | 建议作为主字段或独立文本字段 |
| 状态 | 单选 | 选项：待处理、处理中、已闭环 |
| 查询码 | 文本 | 前端生成的唯一查询码 |
| 客户端请求ID | 文本 | 用于接口幂等和排查重复请求 |
| 提交时间 | 日期 | 脚本写入时间戳 |

记录两个参数：

- `baseId`：AI 表格文档 ID。
- `sheetIdOrName`：数据表 ID 或数据表名称。

`baseId` 可以通过 AI 表格 URL 或文档信息面板获取。字段名必须和 `dingtalk-faas.js` 中的配置一致，否则接口会报字段不存在。

## 4. 创建 H5 微应用

登录钉钉开发者后台：

1. 进入“应用开发”。
2. 进入“企业内部应用”。
3. 创建应用。
4. 应用类型选择“H5 微应用”或“网页应用”。
5. 移动端首页地址填写：

```text
https://你的域名/index.html
```

6. PC 端首页地址可选，填写同一个地址。
7. 上传 `assets/app-icon.png` 作为应用图标。
8. 配置工作台可见范围。
9. 发布应用。

本页面不调用需要鉴权的钉钉 JSAPI，因此不需要实现 `dd.config` 签名流程，也不会从钉钉获取姓名、工号、部门或 uid。

如果当前开发者后台显示“H5 域名白名单”或“安全域名”配置项，请把 H5 静态资源域名加入白名单。不同版本的钉钉后台页面名称可能不同。

如果“开发模式”强制配置服务器出口 IP，需要注意：FaaS 的出口 IP 可能不是固定 IP。若 FaaS 直接调用 `api.dingtalk.com` 并发起获取 accessToken，需要测试当前应用是否触发出口 IP 白名单限制。建议优先使用连接平台内置的钉钉开放接口鉴权或官方连接器能力。

## 5. 申请开放接口权限

在企业内部应用的权限管理中申请：

- AI 表格应用读权限
- AI 表格应用写权限

本版本不需要申请通讯录、手机号、邮箱或部门读取权限。`operatorId` 使用固定服务账号的 `unionId` 即可。

## 6. 准备固定操作人

AI 表格新增记录接口必须传入 `operatorId`，它表示执行写入操作的用户。匿名投诉的写入使用固定的服务账号或管理员账号：

```text
operatorId = 固定服务账号的 unionId
```

不要使用投诉人的 `unionId`。表格里可以不保存该 `unionId`，但平台审计日志会记录该固定账号执行了写入操作。

## 7. 创建连接流

登录钉钉开发者后台：

1. 进入“开放能力”。
2. 进入“连接平台”。
3. 进入“我的连接”。
4. 创建“我的连接流”。
5. 触发事件选择“内置工具 > Webhook”。
6. 添加执行动作，选择 Node.js 脚本连接器或 FAAS 执行动作。
7. 将 `dingtalk-faas.js` 内容粘贴到脚本编辑区。
8. 配置脚本入参和出参。
9. 保存并发布。
10. 复制 Webhook 地址。

推荐入参字段：

```json
{
  "action": "submit",
  "schemaVersion": 1,
  "category": "行政",
  "description": "问题描述",
  "queryCode": "HS-XXXX-XXXX-XXXX-XXXX-XXXX",
  "clientToken": "uuid",
  "clientSubmittedAt": "2026-09-22T06:30:00.000Z"
}
```

推荐出参字段：

```json
{
  "success": true,
  "message": "反馈已写入 AI 表格",
  "recordId": "record-id",
  "queryCode": "HS-XXXX-XXXX-XXXX-XXXX-XXXX",
}
```

## 8. 配置 FaaS 密钥和参数

优先使用连接平台提供的安全配置、连接器鉴权配置或环境变量。

如果当前连接平台支持环境变量，配置：

```text
DINGTALK_APP_KEY
DINGTALK_APP_SECRET
DINGTALK_BASE_ID
DINGTALK_SHEET_ID
DINGTALK_OPERATOR_UNION_ID
```

如果当前版本不支持环境变量，则在 `dingtalk-faas.js` 顶部的 `STATIC_CONFIG` 中填写。该脚本只存在于连接流后台，不能把 AppKey、AppSecret 或脚本地址写进 H5 页面。

`accessToken` 不要做成固定环境变量。脚本每次执行时通过 AppKey 和 AppSecret 动态获取，Token 通常有效期为 2 小时。

## 9. 配置 H5

打开 `outputs/index.html`，修改脚本顶部的配置：

```javascript
const APP_CONFIG = {
  webhookUrl: "粘贴钉钉连接流生成的 Webhook 地址",
  demoMode: false,
  duplicateWindowMs: 60 * 1000
};
```

- `webhookUrl`：连接流 Webhook 地址。
- `demoMode`：本地预览时可保留 `true`；正式部署必须改为 `false`。
- `duplicateWindowMs`：同一浏览器短时间防重复提交窗口。

## 10. CORS、加签和 Webhook 安全

浏览器调用 Webhook 属于跨域请求。连接流 Webhook 必须允许 H5 静态域名发起请求，并允许：

```http
Content-Type: application/json
```

如果不允许 CORS，浏览器页面会显示“网络请求失败”，即使连接流本身可以正常执行。

Webhook 地址会出现在 H5 网络请求中。不要把它当成绝对秘密。建议：

- 使用平台提供的 Webhook 加签能力。
- 不把 Webhook 地址发布到外部网站。
- 设置连接流执行失败告警。
- 发现异常刷量时立即停用或更换 Webhook。
- 定期查看开发者后台的调用量和连接流节点执行量。

如果连接平台要求前端在请求中计算签名，而签名密钥只能放在前端，那么它不能防止有能力查看前端代码的用户滥用。高安全场景需要增加真正的服务端限流或网关。

## 11. 测试清单

部署后至少完成以下测试：

1. 分类和问题描述正常提交并写入 AI 表格。
2. 分类、问题描述为空时不能提交。
3. 问题描述超过 1000 字时不能提交。
4. 验证码错误时不能提交。
5. 连续快速点击提交按钮不会重复发起请求。
6. 从钉钉移动端工作台打开页面时布局正常。
7. Webhook 开启加签后的请求格式与前端一致。
8. 连接流执行失败时，前端能显示失败提示。
9. FaaS 失败时，开发者后台执行记录中有错误信息，但日志中不出现 AppSecret 或完整 accessToken。

## 12. 免费版容量

钉钉标准版的额度会随平台政策调整。按当前公开计费规则，通常每自然月包含：

- API 接口调用量：5,000 次
- Webhook & Stream：3,000 次
- 连接流节点执行量：500 次

纯文字版本每次提交通常只需要 Webhook 触发和 FaaS 写入。准确消耗以开发者后台“资源管理”显示的实时用量为准。免费版更适合试点和小范围使用，不建议直接面向全公司高频开放。若调用量不足，需要购买连接流增购包或升级套餐。

## 13. 当前未实现的功能

- 查询码查询状态。
- 首页动态累计数量。
- 待处理数量的实时统计。
- 投诉处理状态的自动更新。

后续若连接流 Webhook 能够同步返回 FaaS 输出，可以新增 `action=query` 分支，查询结果只返回状态，不返回原始问题描述。若 Webhook 只能异步触发，则查询功能需要改为机器人异步回复，或增加最小后端接口。

当前版本不保存图片，因此也不需要配置附件字段和企业存储文件权限。搜索和查询接口也不应返回原始问题描述。
